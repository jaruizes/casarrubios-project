import { PositionStatusPipePipe } from './position-status-pipe.pipe';

describe('PositionStatusPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PositionStatusPipePipe();
    expect(pipe).toBeTruthy();
  });
});
