name: (Recruitment) Positions Manager Service

on:
  workflow_dispatch:
  push:
    paths:
      - 'project/recruiters/backend/services/applications-updater/**'
  pull_request:
    paths:
      - 'project/recruiters/backend/services/applications-updater/**'

permissions:
  contents: read
  packages: write
  attestations: write
  id-token: write

jobs:
  build:
    uses: ./.github/workflows/quarkus-service.yml
    with:
      image-name: recruitment/applications-updater-service
      service-path: project/recruiters/backend/services/applications-updater
